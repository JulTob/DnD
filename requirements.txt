# Python 3.10+ (3.14 in production). Web app: Shiny only. `make setup` installs into .venv.
shiny==1.6.3

# Tag-Oriented Programming: the TagKit reference implementation, pinned to a commit
# for reproducible installs. The pin moves deliberately, on its own questa (QST-0093.5).
tagkit @ git+https://github.com/JulTob/Tag_Oriented_Programming.git@c7bd3761e8d75de1a13f5f4c0a9a7975ff264b4c
