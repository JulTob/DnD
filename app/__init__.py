# app/__init__.py
# Shared utilities for the project. The web app is Shiny (app.main:app).
# This package provides: app.random, app.character_url, app.static, etc.
# No Flask; use "make run" or "shiny run app.main:app" to run the app.
