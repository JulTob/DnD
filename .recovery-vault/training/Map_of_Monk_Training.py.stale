		name: str,
		min_level: int,
		description,
		chips=(),
		):
	return _path(
			OPEN_HAND,
			name=name,
			min_level=min_level,
			description=description,
			chips=chips,
			)


def _shadow(
		*,
		name: str,
		min_level: int,
		description,
		chips=(),
		):
	return _path(
			SHADOW,
			name=name,
			min_level=min_level,
			description=description,
			chips=chips,
			)


def _elements(
		*,
		name: str,
		min_level: int,
		description,
		chips=(),
		):
	return _path(
			ELEMENTS,
			name=name,