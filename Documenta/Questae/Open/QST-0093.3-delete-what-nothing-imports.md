# QST-0093.3 — Station 3: delete what nothing imports

- **Type:** chore/cleanup
- **Priority:** 🟠 high *(zero risk; closes QST-0003)*
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Simplicity (Monk) · Safety (Paladin)
- **Parent:** QST-0093
- **Related:** QST-0003 (Flask removal) · QST-0017 (orphan sweep) · QST-0081.3 (Finder copies) · QST-0004

---

## 🔍 Diagnosis (what & where)

Measured on 2026-09-06 against `main` after a full-Guild summon: the following have zero importers and are never loaded by the product.

- `AtlasLusoris/CharactersKit.py` (355 lines; the live root is `AtlasActorLudi/CharactersKit.py` with 17 importers).
- `app/routes.py` (Flask, not installed) and the 14 templates under `app/templates/`, including `index 1.0.1.html`; `app/static/glitch-lab.html`; the Finder duplicate `app/static/js/number-input 2.js`.
- `items.py`, `dungeon.py` at the root.
- The 2023 R-Markdown site: `_config.yml`, `_navbar.yml`, `_site.yml`, `DnD.Rproj`, `call.html`, `docs/index.html`, `docs/site_libs/`, `docs/NPCgen.py`, `docs/NPClist.py`, `docs/FLASK_TO_SHINY_MIGRATION.md`.
- The stray `MEMORY.md` at the repository root (an agent's memory index, committed in the accident snapshot `8ec0dec`).
- `AtlasLusoris/Map_of_Species.py` and `AtlasLusoris/Map_of_Backgrounds.py` are **not** on this list: each has one importer (`Map_of_Character_Generation`, `app/routes.py`). `Map_of_Backgrounds` becomes deletable the moment `routes.py` goes; `Map_of_Species` is a fold (QST-0091.1).

## 🧾 Evidence

- `grep -rlE "^\s*(from|import)\s+(items|dungeon)\b"` → 0 files; `app.routes` imported by nothing but itself; `AtlasLusoris.CharactersKit` referenced only in its own `__main__` note.
- `sys.modules` after `import app.main` plus a summon of every Guild: none of the above loaded.

## 🎯 Desired outcome

The files are gone from `main`; compile, smoke, both sweeps and the replay are unchanged; QST-0003 closes. The deletion list above is the whole scope; anything with one importer is a fold and stays.

## 🧭 Notes for the Agora / implementer

- Show the list before the `git rm` (Decree 0007). Nothing here needs a design word; it needs a "go".
- The pre-commit hook caps staged files at 80; this station may need two commits by area (root and docs; app and Atlas).

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —
- **Practice/preference to remember:** —
