# QST-0093.5 — Station 5: the pin moves to TagKit 0.2.0a3

- **Type:** tagkit
- **Priority:** 🔴 urgent *(tagkit; waits only on the upstream merge)*
- **Status:** Open — blocked until upstream PR #4 merges into `main`
- **Owner:** unclaimed
- **Route to:** Methods (Wizard) · Contracts (Warlock) · Architecture (Druid) · Lorekeeper
- **Parent:** QST-0093
- **Related:** QST-0093.1 · QST-0093.2 · QST-0042 · QST-0043 · QST-0051 (what the last ungated re-pin did) · Dialog 0009 proposal 4 · TagKit-Doctrine

---

## 🔍 Diagnosis (what & where)

The project runs TagKit 0.2.0a1 at upstream commit `54d50dd` (2026-08-05, the first commit of branch `codex/tagkit-0.2-alpha`); until 2026-09-07 `requirements.txt` named `c7bd376` (0.1.0), and a fresh install could not boot (found by the QST-0093.8 fresh-venv proof). Upstream `main` is 0.2.0a2 (`7aa839a`) and PR #4 adds Pins back as 0.2.0a3. Everything the project must change to run on a3 is bump-coupled: the new spellings do not exist on a1, so they cannot land early, and the old ones do not exist on a3, so they cannot stay. One branch, one gate.

Dry run on 2026-09-06 (a3 installed in a scratch venv, `Has` shimmed to see past the first error):

| Change | Sites | Files |
|---|---|---|
| `Has(a, T)` → `a in T` | 9 | `AtlasActorLudi/CharactersKit.py` (self-test) |
| `X = Report(v)` → `@Report def X(tag)` | 6 | FighterKit, OrderKit, Origin Feats, and the restored Guild body (station 1) |
| `T.Rip(a)` → `del T[a]` | 4 | `Grimoire_of_Items` |
| `Tag.Label()` → `NAME` or `__name__` | 1 | — |
| `@Pin` on `Declared_Species` and `Background_Audience`; Record inputs keyword-only | 2 roots, 26 input lines | SpeciesKit `declarations.py`, BackgroundKit |
| `@Flag` on identity roots (`Guild`, `Species`, `Heritage`, `Gender`, `Kinship`) so the 66 word probes keep working; the hit-die string ladder becomes a `hit_die` Report | 66 probes | Grimoire_of_Health, GendersKit, Spellcasters, Personality, Kinship |
| Ten `@Post` sites re-read: a failed Postcondition no longer rolls back; `bool(character)` asserted at the end of `summon_player` | 10 | FeaturesKit and kin |

## 🧾 Evidence

- Upstream CHANGELOG 0.2.0a2 "Removed" and "Migration from 0.1" sections; 0.2.0a3 "Pins: Tags as Targets".
- Scratch proof of the SpeciesKit Pin pattern under a3 (QST-0093 evidence).
- QST-0051: the 2026-08-29 re-pin without migrating consumers silently broke the Character root. Hence the gate.

## 🎯 Desired outcome

`requirements.txt` pins the merge commit of PR #4 on upstream `main`. All the rows above are changed in the same branch. Gates before the pin stands: kit self-tests one per process, smoke, both sweeps, the replay rite, and the upstream examples. Reads are measurably faster (a2 restored plain-object attribute cost).

## 🧭 Notes for the Agora / implementer

- Pin the **merge commit**, not the PR head `e53659f`.
- Flags: mark the roots, then delete the twelve-branch `hit_dice` ladder for a Report on each Guild, which is the upstream D&D example verbatim.
- Named failures (`Precondition.Character_Only`) are a gain to spend in the Minion reports in the same branch, if cheap.
- Do not start QST-0091 implementation on this branch; it is the next questa after this one lands.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —
- **Practice/preference to remember:** —
