# QST-0091.4 — Background: one declaration, and nothing outside it spells the name

- **Type:** design
- **Priority:** 🟡 normal *(closest axis to the goal)*
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Architecture (Druid) · Lorekeeper · Simplicity (Monk)
- **Parent:** QST-0091
- **Sidequests:** —
- **Related:** BackgroundKit · `AtlasOfBackgrounds/` · Current-State "Background-specific production should be routed outward" · QST-0047 (skills and tools as Tags)

---

## 🔍 Diagnosis (what & where)

`Build_Background` is already one call: name, audiences, abilities, skills, tools, Origin Feat, title, description, source. Seventy-six Backgrounds are declared through it and no parallel registry exists. What remains outside:

- 12 files name Farmer or Hermeticist outside BackgroundKit and AtlasOfBackgrounds: `Map_of_Stories` (two copies), `Map_of_Titles` (two), `GearKit`, `Grimoire_of_Objects`, `Map_of_Materials`, `Map_of_Exotic_Familiars`, `character_sheet.py`, and the legacy `Map_of_Backgrounds` (read only by the retired Flask routes).
- The audience classification (Player, NonPlayer) is applied as Pins inside `Build_Background`; TagKit 0.2 refuses Pins.
- Background-specific production (a Merchant's wares, a Farmer's tools) is not yet routed outward; Current-State names this as the approved destination not yet complete.

## 🧾 Evidence

- `grep -rlwE 'Hermeticist|Farmer' Atlas* app | grep -vE 'BackgroundKit|AtlasOfBackgrounds'` → 12 files.
- `BackgroundKit.py` line 769: "Construct one complete Background Shape and apply its Pins"; Field reads `Background_Audience[:]`, `Available[:]`, `NPC_Background[:]` at lines 233, 1953, 2260 to 2270.
- Dry run under TagKit 0.2.0a2 reaches BackgroundKit only after GuildKit; its Pin applications will fail the same way SpeciesKit's do.

## 🎯 Desired outcome

One declaration per Background stays the shape. Audience becomes two Reports set by the builder (or two base Tags), and the registries derive from the class tree under `Background`. Anything a Background contributes to stories, titles, gear or materials is declared on the Background itself (a `WARES` or `TRADE` Report, a Kinship-like `Trade` Flag) and the twelve files ask the Tag. `Map_of_Backgrounds` is deleted with the Flask routes.

## 🧭 Notes for the Agora / implementer

- The smallest axis; do it right after Species and use it as the template for how flavour maps ask a Tag instead of a name.
- The Pin replacement here is one builder change; the 76 declarations do not move.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —
- **Practice/preference to remember:** —

---

## 🏛️ Council

> Lorekeeper (Elf Sage): The declaration is finished; the leak is in the stories and titles that still spell "Farmer". Give the Background a word it can be asked for.
> Simplicity Consul (Monk): One builder change closes the Pin problem for 76 files. Take the cheap win first.

**Weighting:** reach 2 × severity 1 = **2** · council leaning: `needs a Dialog`
