# QST-0026.1 — Audit all spell render paths for boxed prose

- **Type:** bug
- **Priority:** 🟠 high
- **Status:** Working
- **Owner:** Agent (Julio's request)
- **Route to:** Readability Consul (Barbarian), Architecture Consul (Druid)
- **Parent:** QST-0026
- **Sidequests:** —
- **Related:** QST-0026 · QST-0002

---

## 🔍 Diagnosis (what & where)
Spell text is boxed because **upstream generators embed spell HTML inside `npc-textbox` divs**, and the Shiny sheet faithfully renders that HTML inside prose sections. The defect is not isolated to one cantrip (`Friends`) — it is a systemic pattern across feature and spellcaster string builders.

---

## 🧾 Evidence — render path inventory

### PC character sheet (`build_character_sheet`, `shiny_app.py`)

| Path | Source | Formatter | Final container | Status |
|------|--------|-----------|-----------------|--------|
| **A. Features** | `feat.description` from `char.features` | `ui.markdown(f"**{name}.** {desc}")` | `.sheet-section` prose | ❌ **boxed** when `desc` contains `<div class='npc-textbox'>{Spell}</div>` |
| **B. Spells section** | `data["Spellcaster"]` | `spellbook_prose()` → `_prose()` | `.sheet-section` prose | ⚠ **prose-compliant but incomplete** — lists spell **names only**, no definitions |
| **C. Backstory / Equipment / etc.** | other fields | `prose_section` + `_prose` | `.sheet-section` | ✅ prose (not spell-related) |

### Root cause for `Friends` cantrip (confirmed)

**Magic Initiate** feat (`AtlasLusoris/Grimoire_of_Features/__init__.py`, `MagicInitiate()` ~L1449–1461):

```python
bullets = "".join(f"<div class='npc-textbox'>{s}</div>" for s in chosen_cantrips)
# ...
_feat.description = (... + bullets + ...)
```

When `chosen_cantrips` includes `Friends`, `str(Friends)` expands to full spell HTML (`Lodge_of_Spells.Spell.__str__`, ~L1803–1821: name, cantrip label, definition). That entire block is wrapped in `npc-textbox` and injected into the **Features** section.

**User-visible symptom:** `Friends` + cantrip header + definition appear inside a bordered box under Features (e.g. "Magic Initiate (Bard)").

### Upstream boxed-spell emitters (data layer — primary fix targets)

| File | Occurrences | Pattern | Status |
|------|-------------|---------|--------|
| `AtlasLusoris/Grimoire_of_Spellcasters.py` | **66** | `f'<div class="npc-textbox">{spell}</div>'` in class `__str__` / HTML builders (Wizard, Druid, Cleric, Warlock, Bard, etc.) | ❌ boxed |
| `AtlasLusoris/Grimoire_of_Features/__init__.py` | **11** | Magic Initiate, Light Bearer, invocations, fighting styles (Druidic Warrior, Blessed Warrior) | ❌ boxed |
| `AtlasLusoris/Map_of_Classes/Training/Bard.py` | 3 | subclass feature descriptions | ❌ boxed |
| `AtlasLusoris/Map_of_Classes/Training/Monk.py` | 3 | subclass feature descriptions | ❌ boxed |
| `AtlasLusoris/Map_of_Classes/Training/Warlock.py` | 2 | subclass feature descriptions | ❌ boxed |
| `AtlasLusoris/Map_of_Classes/Training/Rogue.py` | 1 | subclass feature descriptions | ❌ boxed |
| `AtlasLudus/Map_of_Languages.py` | 1 | unrelated / verify | ⚠ |
| `shiny_app.py` | 18 | presentation CSS/selectors; `spellbook_prose` does not box | ✅ presentation layer OK |

**Total `npc-textbox` in spell-related generators: ~87 occurrences across 7 files.**

### Spell object string format (`AtlasMagia/Lodge_of_Spells.py`)

`Spell.__str__` (~L1803) returns HTML with `<h4>`, level label, and full `definition` — suitable for prose, but currently **nested inside `npc-textbox`** by callers.

### NPC sheet (`build_npc_sheet`, `shiny_app.py`)

| Path | Source | Formatter | Container | Status |
|------|--------|-----------|-----------|--------|
| Spellcasting block | `npc.spells` → `Map_of_Magic.Magic()` | `_prose()` in `prose_block` | `.npc-prose` | ✅ plain text via `Entry.string` (no `npc-textbox` in NPC magic path) |
| Class spellcasters (if used elsewhere) | `Grimoire_of_Spellcasters.__str__` | HTML with `npc-textbox` | varies | ❌ boxed when that HTML is emitted |

NPC PC-path divergence: NPC spell list uses `Map_of_Magic.Magic()` (text entries); PC spell **definitions in features** use boxed HTML from Features/Spellcasters grimoires.

### Legacy Flask template (out of Shiny scope but same pattern)

- `app/templates/npc.html` — multiple `.npc-textbox` wrappers (QST-0003 retirement track).

---

## 🎯 Desired outcome
- [x] Full path inventory with classification
- [x] Root cause identified for `Friends` cantrip
- [x] Upstream emitters enumerated for QST-0026.2

---

## 🧭 Notes for QST-0026.2 (implementation)

**Do not patch only `Friends`.** Fix the contract at the source:

1. **Stop wrapping spells in `npc-textbox`** in `Grimoire_of_Spellcasters.py` and `Grimoire_of_Features/__init__.py` (and Training files).
2. **Emit spell prose** via `Spell.describe()` / markdown-friendly text, or a shared `spell_to_prose(spell)` helper in AtlasMagia or AtlasVenustas.
3. **Expand `spellbook_prose()`** in `shiny_app.py` to include full definitions for the Spells section (currently names-only).
4. **Features section:** spell grants (Magic Initiate, species cantrips, invocations) should reference spells in prose, not embed boxed HTML.

**Presentation layer (`shiny_app.py`) is not the primary bug** — it correctly renders markdown prose; the poison is in the HTML baked into `feature.description` and spellcaster `__str__` output.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** QST-0026
- **What changed:** Audit complete — see inventory above.
- **Practice/preference to remember:** Spell text is domain data; presentation boxes belong only to short stats, never to spell definitions.
