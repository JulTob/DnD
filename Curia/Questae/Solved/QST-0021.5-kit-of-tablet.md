# QST-0021.5 — Kit_of_Tablet + app/static/js/tablet.js

- **Type:** refactor
- **Priority:** 🟡 normal
- **Status:** Solved (2026-07-09)
- **Owner:** unclaimed
- **Route to:** Workshop Consul (Artificer), Flow Consul (Bard)
- **Parent:** QST-0021
- **Sidequests:** —
- **Related:** QST-0021.4

---

## 🔍 Diagnosis (what & where)
The home page generator "tablet" carousel (rotate panels, dots, prev/next, auto-advance) lives as `HOME_SCRIPT` in `shiny_app.py` L731–848, injected at L1687. It targets `#generator-tablet`, `.tablet-rotator`, `.generator-panel`, etc.

## 🧾 Evidence
- `HOME_SCRIPT` IIFE with `initGeneratorTablet()`, panel index, auto-timer
- Home UI markup defined elsewhere in `shiny_app.py` (tablet structure)

## 🎯 Desired outcome

### New files
| File | Role |
|------|------|
| `app/static/js/tablet.js` | Extracted `HOME_SCRIPT` logic |
| `AtlasVenustas/Kit_of_Tablet.py` | Script tag for Shiny head |

### `Kit_of_Tablet.py` contract
```python
def tablet_head_tags() -> list:
    """Script tags for the home generator tablet carousel."""
```

### `shiny_app.py` changes
- Remove `HOME_SCRIPT` constant
- Wire `tablet_head_tags()` in head

## 🧭 Notes for the Agora / implementer

### Must preserve
- [ ] Home page tablet rotates between generator panels (Character / NPC / List)
- [ ] Dot indicators and prev/next buttons work
- [ ] Auto-advance timer still runs; pauses on interaction if it does today
- [ ] `dataset.ready` guard prevents double-init

### Optional (out of scope unless trivial)
- Move tablet **markup** into `Kit_of_Tablet.tablet_panel()` — only if it shrinks `shiny_app.py` without coupling to server state. Server inputs must stay in `shiny_app.py`.

### Depends on
- QST-0021.4 complete (prior extractions landed)

### Acceptance criteria
- [ ] No `HOME_SCRIPT` in `shiny_app.py`
- [ ] Home tablet UX unchanged

## ✅ Resolution
- **Decided by:** Julio (run-the-train ruling, 2026-07-09)
- **What changed:** commits `0194aa2` + `19d1643`. HOME_SCRIPT extracted to `app/static/js/tablet.js`, served by `Kit_of_Tablet.tablet_head_tags()`; same head position, behavior unchanged.
- **Practice/preference to remember:** static client logic belongs in a lintable, cacheable file; the Kit only serves the tag. Dynamic scripts (loader symbol injection) may stay Python-built - that is the Kit_of_Loader exception, not the rule.

---

## 🏛️ Council
> Flow Consul (Bard): The tablet is the front door — *utilitas* demands it stay smooth after extraction.
> Workshop Consul (Artificer): Name it Kit_of_Tablet, not Kit_of_Home — one widget, one kit.

**Weighting:** reach 1 × severity 2 = **2** · council leaning: `build`
