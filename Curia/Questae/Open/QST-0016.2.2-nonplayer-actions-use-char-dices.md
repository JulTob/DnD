# QST-0016.2.2 — NonPlayer Actions draw from `char.dices`

- **Type:** refactor
- **Priority:** 🟠 high
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Druid, Monk, Warlock → QST-0016.2
- **Parent:** QST-0016.2
- **Sidequests:** —
- **Related:** Decree 0002 §4 · `AtlasAlusoris/Grimoire_of_NPC.py` · QST-0016.3

---

## 🔍 Diagnosis (what & where)
NonPlayer Tag Actions repeatedly call `random.seed(npc.seed)` / `random.seed(npc._seed)` then draw from `app.random`. That fights the owned bag and reseeds a shared stream mid-composition.

## 🧾 Evidence
Dozens of `random.seed(...)` call sites in `Grimoire_of_NPC.py`; draws go through the module RNG, not `npc.dices`.

## 🎯 Desired outcome
NonPlayer Actions use `npc.dices` / `npc.Dice` only. Delete per-Action `random.seed` calls. Reseed discipline is `~npc` at Action entry when a fresh stream from `seed` is required — never a second global bag.

## 🧭 Notes for the Agora / implementer
Prefer after or alongside `.2.1` so the Player pattern is proven. Soft-fail story / Finish_Awakening tensions stay in QST-0016.3 — this quest is RNG only.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —

---

## 🏛️ Council
> Monk Consul: Kill the seed-per-property habit; it is the opposite of one stream.
> Warlock Consul: Same contract as Player — Agent carries the Dice.

**Weighting:** reach 2 × severity 3 = **6** · council leaning: `build`
