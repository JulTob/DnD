# QST-0016.2.6 — Retire `app.random` from the Character path

- **Type:** chore/cleanup
- **Priority:** 🟡 normal
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Druid, Workshop, Warlock → QST-0016.2
- **Parent:** QST-0016.2
- **Sidequests:** —
- **Related:** `app/random.py` · `shiny_app.py` summoners · QST-0016.2.1–.2.5 · QST-0016.6

---

## 🔍 Diagnosis (what & where)
`app.random` was a process-wide game bag — useful before owned Dice, now a second entropy authority. Summoners and leftover Character-path imports can still poke it, undoing reproducibility even when Grimoires look clean.

## 🧾 Evidence
`app/random.py` mirrors stdlib on a module `_rng`; Character path historically seeded it from share links / summoners. Sibling sidequests remove draws; this quest removes the remaining authority on that path.

## 🎯 Desired outcome
Character summon → awaken → sheet path never imports or seeds `app.random`. `__invert__` stays bag-only (`char.dices`). Whether `app.random` survives for non-Character domains is a separate decision — out of scope unless Julio widens it.

## 🧭 Notes for the Agora / implementer
Gate behind `.2.1`–`.2.5` completion (or proof those paths no longer touch it). Pair with QST-0016.6 golden-master once the graph is clean.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —

---

## 🏛️ Council
> Warlock Consul: Two bags means no pure function of seed — retire the spare on this path.
> Druid Consul: Do not delete `app.random` for the whole repo in this quest; Character path first.

**Weighting:** reach 2 × severity 2 = **4** · council leaning: `build`
