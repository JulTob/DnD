# QST-0021.2 — Kit_of_Loader + app/static/js/loader.js

- **Type:** refactor
- **Priority:** 🟠 high
- **Status:** Solved *(see `Solved/QST-0021.2-kit-of-loader.md`)*
- **Owner:** unclaimed
- **Route to:** Workshop Consul (Artificer), Flow Consul (Bard), Readability Consul (Barbarian)
- **Parent:** QST-0021
- **Sidequests:** —
- **Related:** QST-0021.1 · QST-0021.3

---

## 🔍 Diagnosis (what & where)
The summon loader (~180 lines) lives as `LOADER_SCRIPT` string literal in `shiny_app.py` L849–1027, plus markup at L1899–1906. It controls golden rune overlay, status typing, Shiny `set_loader` handler, mutation observers on output roots, and `window.summonLoader` API.

## 🧾 Evidence
- Injected at L1688: `ui.tags.script(ui.HTML(LOADER_SCRIPT))`
- Markup: `#loader`, `.sol-layer`, `.orbit-layer`, `#loader-message`
- Symbol arrays hardcoded in JS; `Lodge_of_Symbols` ready after QST-0021.1

## 🎯 Desired outcome

### New files
| File | Role |
|------|------|
| `AtlasVenustas/Kit_of_Loader.py` | Full loader Logic |


### `shiny_app.py` changes
- Remove `LOADER_SCRIPT` string constant
- Replace inline loader markup + script with `loader_panel()` and `loader_head_tags()`
- Preserve: `summonLoader`, `set_loader` custom message, auto-hide when loaded and sheet is up, status lines, action button IDs

### Symbol wiring
Prefer injecting `SOL_SYMBOLS` / `PLANET_SYMBOLS` from `Lodge_of_Symbols` into the page (inline `window.__LOADER_SYMBOLS = …` or data attributes) so JS does not duplicate pools.

## 🧭 Notes for the Agora / implementer

### Must preserve (regression checklist)
- [ ] Click "Generate Character" → loader appears with rotating runes + typing status
- [ ] Loader hides when `#character_result` / `#npc_result` / `#npc_list_result` mutates
- [ ] `shiny:idle`, `shiny:disconnected`, `hashchange` still hide loader
- [ ] `window.summonLoader.show()` / `.hide()` still callable

### Shiny static serving
Confirm how this project serves `app/static/js/` — use existing pattern (`ui.head_content` + static path, or `ui.tags.script(src=…)`). Match whatever works for `style.css` today.

### Do NOT
- Change loader CSS (that is QST-0021.3)
- Touch Flask `base.html` in this slice

### Depends on
- QST-0021.1 complete (`Lodge_of_Symbols` exists)

## ✅ Resolution
*(pending)*

---

## 🏛️ Council
> Flow Consul (Bard): The loader is the user's first ritual — extraction must be behavior-identical, not a rewrite.
> Workshop Consul (Artificer): One JS file, one Kit. Symbols from Lodge, not copy-pasted.

**Weighting:** reach 2 × severity 2 = **4** · council leaning: `build`
