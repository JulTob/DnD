# QST-0081.6 — Dual prewipe Grimoire pycs (GendersKit import)

- **Type:** recovery / evidence fork
- **Priority:** 🟡 normal
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Julio, recovery board
- **Parent:** QST-0081
- **Sidequests:** —
- **Related:** QST-0081.5 · `AtlasActorLudi.GendersKit`

---

## 🔍 Diagnosis (what & where)

`.recovery-vault/_prewipe-pyc-mirror/AtlasActorLudi/AtlasAlusoris/` holds two `Grimoire_of_NPC` pycs. They are not the same import list. Recovery used the vault copy (the larger one, which imports `Gender_Reveal` from `AtlasActorLudi.GendersKit`).

## 🧾 Evidence

| File | Bytes | Module-level `AtlasActorLudi.GendersKit` |
|------|------:|------------------------------------------|
| vault `Grimoire_of_NPC.cpython-314.pyc` / prewipe `2c4197c008a1.pyc` | 30523 | **yes** — `from AtlasActorLudi.GendersKit import Gender_Reveal` |
| prewipe `6a2254a38312.pyc` | 30511 | **no** — `Gender_Reveal` is still a name inside `Finish_Awakening`, but the module does not import `GendersKit` |

Both copies still *call* `Gender_Reveal(npc, npc.gender)` after `SetGender`. The smaller file must have resolved that name some other way (or would NameError). Do not mix the two.

Other dual prewipe pairs in the same folder (`Map_of_Races` 74114 vs 73993, `RaceKit`, `Map_of_NonPlayer_Features`) differ in size only: nested `co_names` / string sets matched; recovery used the vault file.

## 🎯 Desired outcome

Julio confirms the vault + GendersKit import is the kept rite. The smaller Grimoire pyc stays evidence, not a merge source.

## 🧭 Notes for the Agora / implementer

Restored source already follows `2c4197c008a1` / vault. Do not delete the smaller pyc from the mirror.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —
- **Practice/preference to remember:** —

---

## 🏛️ Council

> Recovery agent (Grok): Restored the vault Grimoire (GendersKit import). The 12-byte-smaller twin is a second compile, not a second design I can merge.
> Artificer Consul (Workshop): Keep the import that names the live kit. The smaller pyc is a fingerprint, not a patch.

**Weighting:** reach 2 × severity 2 = **4** · council leaning: `build` (keep vault)
