# QST-0016.2.1 — Player rites draw from `char.dices`

- **Type:** refactor
- **Priority:** 🟠 high
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Druid, Monk, Warlock → QST-0016.2
- **Parent:** QST-0016.2
- **Sidequests:** —
- **Related:** Decree 0002 §4 · Dialog 0007 · `AtlasLusoris/Grimoire_of_Characters.py`

---

## 🔍 Diagnosis (what & where)
Player awakening rites already call `~char`, then still draw from `app.random` (`choice`, `choices`, `randint`, `shuffle`) for species, class, subclass, background, and ability rolls. The owned bag is unused on the hot path.

## 🧾 Evidence
`Grimoire_of_Characters.py` imports `app.random`; rites after `~char` call `random.choices` / `random.choice` / `random.randint` / `random.shuffle`.

## 🎯 Desired outcome
Every Player rite that needs entropy uses `char.Dice` / `char.Roll` for die formulas and `char.dices.*` for selection APIs. No `app.random` / `import random` in this Grimoire.

## 🧭 Notes for the Agora / implementer
First slice of the Dice migration — small surface, already has `~char`. Do not widen into NonPlayer or Maps here; those are sibling sidequests.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —

---

## 🏛️ Council
> Warlock Consul: This is the litmus for “seed owns the life.” If Player rites still touch the global bag, `~char` is theater.
> Monk Consul: One Grimoire, one swap pattern — finish this before touching NonPlayer’s seed-per-Action noise.

**Weighting:** reach 2 × severity 3 = **6** · council leaning: `build`
