# QST-0081.8 — vault `app.main` calls 3-arg `alusoris_page_ui`; live page takes 2

- **Type:** design conflict (live vs vault)
- **Priority:** 🟡 normal
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Julio, Artificer (Workshop)
- **Parent:** QST-0081
- **Sidequests:** —
- **Related:** QST-0081.5

---

## 🔍 Diagnosis (what & where)

Vault `app/main.py` builds `_PAGE_VIEWS[Page.ACTOR_LUDI_ALUSORIS]` as:

`alusoris_page_ui(RACE_CHOICES, NONPLAYER_GUILD_CHOICES, NONPLAYER_BACKGROUND_CHOICES)`

Live `app/pages/actor_ludi/alusoris.py` `page_ui` takes only `race_choices` and `nonplayer_background_choices`. There is no vault pyc for that page — only the recovered source. Importing restored `app.main` raises `TypeError: page_ui() takes 2 positional arguments but 3 were given`.

The home page still has a NonPlayer guild selector (`npc_class`). The sheet page does not.

## 🧾 Evidence

Vault `app/main.cpython-314.pyc` `CALL 3` at the `alusoris_page_ui` site. Live `page_ui` signature is two positional parameters. No `.recovery-vault` pyc named `alusoris`.

## 🎯 Desired outcome

Julio chooses whether the NPC sheet grows a guild selector (vault main) or `app.main` drops `NONPLAYER_GUILD_CHOICES` (live page). Do not guess-merge.

## 🧭 Notes for the Agora / implementer

Restored `app/main.py` keeps the vault 3-arg call so the bytecode still tells the truth. Do not add a discarded unused argument to hide the fork.

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —
- **Practice/preference to remember:** —

---

## 🏛️ Council

> Recovery agent (Grok): Vault composition root still threads guild choices into the Alusoris sheet. Live sheet UI dropped that control. Need Julio.
> Paladin Consul (Safety): Silently ignoring the guild argument would make Home's guild pick look connected when the sheet never uses it.

**Weighting:** reach 2 × severity 2 = **4** · council leaning: `needs a Dialog`
