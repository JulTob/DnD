# QST-0081.2 — Authored recovery left the same Imprint twice

- **Type:** bug
- **Priority:** 🟡 normal
- **Status:** Open
- **Owner:** unclaimed
- **Route to:** Monk (Readability), Cleric (Repair), Julio
- **Parent:** QST-0081
- **Sidequests:** —
- **Related:** QST-0081 · QST-0045 (duplicate chip declarations)

---

## 🔍 Diagnosis (what & where)

Two already-restored Species files carry the same block twice. Runtime is idempotent (the second write overwrites the first with the same values). This is not a vault-vs-live design conflict; it is paste duplication left in authored source.

1. `AtlasActorLudi/SpeciesKit/Dwarves/traits.py` — `Dwarven_Toughness.Set_Dwarven_Toughness` writes `bonus_health_sources` / `bonus_health_per_level` twice (roughly lines 81–94, then 95–111).
2. `AtlasActorLudi/SpeciesKit/Aasimar/resolution.py` — `_revelation_options` assigns the shared Revelation Records twice (roughly `celestial_revelation_extra_damage` through `celestial_revelation_condition`).

## 🧾 Evidence

The Dwarf file comments "Granted, not merely recorded" appear twice in a row over the same `sources = dict(...)` block. The Aasimar file comments "There is deliberately no ``damage_type``" appear twice over the same assignments. Neither pair disagrees with itself.

## 🎯 Desired outcome

Each Imprint / projection writes each Record once. Behaviour unchanged.

## 🧭 Notes for the Agora / implementer

Do not treat these as two competing designs. Delete the second copy. Do not widen into a FeaturesKit sweep (that is QST-0081.1).

---

## ✅ Resolution (filled when Solved)
- **Decided by:** —
- **What changed:** —
- **Practice/preference to remember:** —

---

## 🏛️ Council

> Recovery agent (Grok): Julio said recreate/expand/review is allowed, but a conflict starts a questa. These are not conflicts — they are the same block twice. Still minted so a later pass does not "fix" them as if two authors disagreed.
> Monk Consul (Readability): Cut the second copy. Keep the comment once.

**Weighting:** reach 1 × severity 1 = **1** · council leaning: `build`
